# =================================================================
# 🦅 MataElang (MatEl) OS - Configuration Template
# =================================================================
# Rename this file to '.env' and fill in your details.

# --- 💾 Database Configuration ---
# Default is SQLite (local file). 
# For PostgreSQL/MySQL, provide the full connection string.
DATABASE_URL=sqlite:///./matel.db

# --- 🔐 Security ---
# Secret key used for JWT token signing. Generate a strong random string!
# Example: openssl rand -hex 32
SECRET_KEY=b4bb9013c1cw90312nds902319023ndas09312

# --- 🛰️ Alerting: Telegram (Optional) ---
# To get alerts via Telegram, create a bot via @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# --- 📧 Alerting: Email (SMTP) ---
# Required for user verification and critical alerts.
# Recommended: Gmail (with App Password), SendGrid, or Mailtrap.
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
SMTP_FROM=noreply@example.com

# --- 🌐 Networking ---
# The host and port the application will bind to.
# Note: 'run_matel.py' defaults to 8000.
APP_HOST=0.0.0.0
APP_PORT=8000

# =================================================================
# Note: Ensure Python and all dependencies from requirements.txt 
# are installed before running the application.
# =================================================================
